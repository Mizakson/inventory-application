const { render } = require("ejs")
const db = require("../db/queries")

async function addPlayerFormGet(req, res) {
    
}

async function addPlayerForPost(req, res) {
    return 0
}

module.exports = {
    addPlayerFormGet,
    addPlayerForPost
}